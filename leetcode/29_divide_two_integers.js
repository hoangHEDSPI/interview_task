/**
 * @param {number} dividend
 * @param {number} divisor
 * @return {number}
 */
var divide = function(dividend, divisor) {
    // bit-manipulation for this problem

    // solve overflow case
    if (dividend ==)
    let is_minus = ((dividend < 0 && divisor > 0) || (dividend > 0 && divisor < 0)) ? true : false;
    
    let hold_divisor = divisor;
    let result = 0;

    // dividend = Math.abs(dividend); --> bad code
    // divisor = Math.abs(divisor);  --> bad code

    while (dividend >= divisor) {

    }

};